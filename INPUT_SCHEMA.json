{
  "title": "Huizen scraper input",
  "description": "Scrapet woningen van Funda en Pararius met Playwright (Python).",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "plaats": {
      "title": "Plaats / regio",
      "type": "string",
      "description": "Bijv. amsterdam, utrecht, noord-holland. Wordt gebruikt in de URL voor Funda en Pararius.",
      "default": "amsterdam",
      "editor": "textfield"
    },
    "paginas": {
      "title": "Aantal pagina's per platform",
      "type": "integer",
      "description": "Hoeveel pagina's per platform (Funda, Pararius) moeten worden gescrapet.",
      "minimum": 1,
      "maximum": 100,
      "default": 5,
      "editor": "number"
    },
    "platforms": {
      "title": "Platforms om te scrapen",
      "type": "array",
      "description": "Kies één of meerdere platforms.",
      "items": {
        "type": "string",
        "enum": ["FUNDA", "PARARIUS"],
        "enumTitles": ["Funda", "Pararius"]
      },
      "uniqueItems": true,
      "minItems": 1,
      "default": ["FUNDA", "PARARIUS"],
      "editor": "select"
    },
    "headless": {
      "title": "Headless browser",
      "type": "boolean",
      "description": "Gebruik een headless browser (true) of zichtbare browser (false). Op Apify is headless sterk aangeraden.",
      "default": true,
      "editor": "checkbox"
    }
  },
  "required": ["plaats"]
}
